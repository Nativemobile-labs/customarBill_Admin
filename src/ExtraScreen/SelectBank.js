import { StyleSheet, Text, View, SafeAreaView, ScrollView } from 'react-native'
import React from 'react'

export default function SelectBank() {
  return (
    <SafeAreaView>
      <Text>SelectBank</Text>
    </SafeAreaView>
  )
}

const styles = StyleSheet.create({})